package com.company.delivery.infrastructure.config;

/**
 * Web MVC configuration.
 *
 * <p>This class configures Spring MVC settings including CORS, converters,
 * and interceptors.</p>
 *
 * <h2>Features (Placeholder)</h2>
 * <ul>
 *   <li>CORS configuration for frontend origins</li>
 *   <li>Custom message converters</li>
 *   <li>Request interceptors</li>
 *   <li>Exception handlers</li>
 * </ul>
 *
 * <p><strong>TODO:</strong> Implement web configuration when requirements are finalized.</p>
 */
// @Configuration
public class WebConfig {

    // ==========================================================================
    // PLACEHOLDER - No implementation yet
    // ==========================================================================
    //
    // Future configuration may include:
    //
    // @Bean
    // public CorsConfigurationSource corsConfigurationSource() {
    //     CorsConfiguration configuration = new CorsConfiguration();
    //     configuration.setAllowedOrigins(List.of(
    //         "http://localhost:3000",  // React Native web
    //         "http://localhost:19006"  // Expo
    //     ));
    //     configuration.setAllowedMethods(List.of("GET", "POST", "PUT", "DELETE", "OPTIONS"));
    //     configuration.setAllowedHeaders(List.of("*"));
    //     configuration.setAllowCredentials(true);
    //
    //     UrlBasedCorsConfigurationSource source = new UrlBasedCorsConfigurationSource();
    //     source.registerCorsConfiguration("/api/**", configuration);
    //     return source;
    // }
    // ==========================================================================

}
